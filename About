<!-- About/Profile Section -->
<section id="about">
  <div class="bio-card">
    <h2 class="typing">Dr. Ikraam Sadek</h2>
    <p>
      Lecturer in Software Engineering, passionate about empowering students with real-world skills.
    </p>
    <div class="badges">
      <span class="badge">🎓 Mentor</span>
      <span class="badge">💻 Innovator</span>
      <span class="badge">🌍 Visionary</span>
    </div>
  </div>
</section>

<style>
/* Section styling */
#about {
  display: flex;
  justify-content: center;
  align-items: center;
  padding: 60px 20px;
  background: #0d1117;
  color: #fff;
}

/* Bio card with glowing border */
.bio-card {
  max-width: 600px;
  padding: 30px;
  border-radius: 15px;
  background: #161b22;
  position: relative;
  animation: glow-border 2s infinite alternate;
}

/* Glowing border animation */
@keyframes glow-border {
  from { box-shadow: 0 0 10px #00f7ff; }
  to { box-shadow: 0 0 25px #00f7ff, 0 0 50px #00f7ff; }
}

/* Typing effect */
.typing {
  font-size: 1.8em;
  font-family: monospace;
  border-right: 3px solid #00f7ff;
  white-space: nowrap;
  overflow: hidden;
  width: 0;
  animation: typing 4s steps(30, end) forwards, blink 0.7s infinite;
}

@keyframes typing {
  from { width: 0; }
  to { width: 100%; }
}

@keyframes blink {
  50% { border-color: transparent; }
}

/* Personality badges */
.badges {
  margin-top: 20px;
  display: flex;
  gap: 10px;
}

.badge {
  background: #00f7ff;
  color: #000;
  padding: 8px 15px;
  border-radius: 20px;
  font-weight: bold;
  transition: transform 0.3s ease;
}

.badge:hover {
  transform: scale(1.1);
}
</style>
